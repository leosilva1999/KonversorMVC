@model Konversor.ViewModels.IncreasePercentageOnValueViewModel

<form asp-action="CalculateIncreasePercentageOnValue" method="post">
    <ul class="list-unstyled ps-0">
        <div class="d-flex align-items-center justify-content-center gap-2 flex-wrap" style="margin-bottom:12px;">

            <span>Tenho um valor de</span>

            <input asp-for="ValueToIncrease"
                   class="form-control"
                   style="width:120px;"
                   placeholder="--">

            <span>e quero aumentar em</span>

            <div class="input-group" style="width:120px;">
                <input asp-for="IncreasePercentage" class="form-control" placeholder="--">
                <span class="input-group-text">%</span>
            </div>

            <span>Qual o resultado?</span>

        </div>
        @if (Model.Result.HasValue)
        {
            <div class="d-flex align-items-center justify-content-center gap-2 flex-wrap">
                <div class="alert alert-success mt-3" style="width:120px;">
                    <span>Resultado:</span>
                    <strong>@Model.Result</strong>
                </div>
            </div>
        }
        @if (!string.IsNullOrEmpty(Model.Error))
        {
            <div class="d-flex align-items-center justify-content-center gap-2 flex-wrap">
                <div class="alert alert-warning mt-3">
                    <span>Erro:</span>
                    <strong>@Model.Error</strong>
                </div>
            </div>
        }
        <button class="btn btn-primary">Calcular</button>
    </ul>
</form>
