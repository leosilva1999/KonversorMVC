@using Konversor.ViewModels
@model RestoDivisaoViewModel

<div class="text-center">
    <h1 class="display-4">Resto da Divisão</h1>

    <form asp-action="CalculateRestoDivisao" method="post">
        <ul class="list-unstyled ps-0">
            <div class="d-flex align-items-center justify-content-center gap-2 flex-wrap" style="margin-bottom:12px;">

                <span>Qual o resto da divisão de</span>

                <input asp-for="Dividendo"
                       class="form-control"
                       style="width:120px;"
                       placeholder="--">

                <span>por</span>

                <input asp-for="Divisor"
                       class="form-control"
                       style="width:120px;"
                       placeholder="--">

                <span>?</span>

            </div>
            @if (Model.Result.HasValue)
            {
                <div class="d-flex align-items-center justify-content-center gap-2 flex-wrap">
                    <div class="alert alert-success mt-3" style="width:120px;">
                        <span>Resultado:</span>
                        <strong>@Model.Result</strong>
                    </div>
                </div>
            }
            @if (!string.IsNullOrEmpty(Model.Error))
            {
                <div class="d-flex align-items-center justify-content-center gap-2 flex-wrap">
                    <div class="alert alert-warning mt-3">
                        <span>Erro:</span>
                        <strong>@Model.Error</strong>
                    </div>
                </div>
            }
            <button class="btn btn-primary">Calcular</button>
        </ul>
    </form>
</div>
